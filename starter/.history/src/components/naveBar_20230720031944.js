import { CartIcon } from '../icons'
import useSelector from 'react-redux'

const NavBar = () => {

  return (
    <section>
      <nav>
        <div className='nav-center'>
          
        </div>
      </nav>
    </section>
  )
}
import { CartIcon } from '../icons'
import useSelector from 'react-redux'

const NavBar = () => {

  return (
    <section>
      <nav>
        <div className='nav-center'>
          <h3></h3>
        </div>
      </nav>
    </section>
  )
}